# Secret values are injected locally or by CI and must never be committed.
# Google Maps Platform API key used by the Maps SDK / Maps Compose.
MAPS_API_KEY=

# Optional: only set this when the app uses Gemini AI APIs.
GEMINI_API_KEY=

# Optional Firebase App Check debug token for local development only.
# FIREBASE_APPCHECK_DEBUG_TOKEN=
